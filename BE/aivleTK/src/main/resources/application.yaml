server:
  port: ${PORT:8080}
spring:
  application:
    name: aivleTK
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  # 마지막날에 db 주소 바꿔야한다고 말좀 해주세요 이거 공용이라
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
app:
  cors:
    allowed-origins: "http://localhost:5173,https://witty-sky-068602700.2.azurestaticapps.net/,https://aivleinsight.xyz/"
  bid:
    fetch:
      schedule: "0 0 0 * * *" # 매일 자정 공고 받아오기
      on-startup: true # 서버 시작 시 공고 받아 오기

jwt:
  secret:
    key: ${JWT_SECRET_KEY}
azure:
  storage:
    connection-string: ${AZURE_STORAGE_KEY}
    container-name: "uploads"
