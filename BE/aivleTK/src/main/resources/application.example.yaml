# Copy this file to application-local.yaml for local development. Never commit
# real credentials; production values are injected through the platform secret store.
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/aivleTK?useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: local_user
    password: replace-me

jwt:
  secret:
    key: replace-with-a-random-secret-of-at-least-32-bytes

azure:
  storage:
    connection-string: replace-me
